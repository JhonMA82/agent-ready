#!/bin/sh
# Deterministic: renders generated/routes/<id>.json from presets/manifest.json.
# No semantic judgment — the audit classifies this as script-level context.
set -eu
echo "routes generated from presets/manifest.json"
